---
phase: 01-project-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .gitignore
  - README.md
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "Code is pushed to a GitHub repository"
    - "Pushing to main branch triggers automatic Vercel deployment"
    - "User can visit a live Vercel URL and see the F1-themed app"
  artifacts:
    - path: ".git/config"
      provides: "Git repo with GitHub remote configured"
      contains: "github.com"
    - path: ".gitignore"
      provides: "Standard Next.js gitignore"
      contains: "node_modules"
  key_links:
    - from: "GitHub repository (main branch)"
      to: "Vercel deployment"
      via: "Vercel GitHub integration auto-deploy"
      pattern: "push to main triggers build"
---

<objective>
Push the project to GitHub and set up Vercel auto-deployment so every push to main goes live.

Purpose: Establish the CI/CD pipeline so all future phase work is automatically deployed on merge to main.
Output: A live Vercel URL serving the F1 tracker app, auto-deploying on push.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git repo and push to GitHub</name>
  <files>
    .gitignore
  </files>
  <action>
1. Verify `.gitignore` exists and includes standard Next.js entries (node_modules, .next, out, etc.). If create-next-app already created it, verify it's correct. If the project already has a .git directory from the planning setup, that's fine -- just ensure the Next.js source is committed.

2. Stage all project files (excluding .planning/ if desired, but it's fine to include it).

3. Create initial commit with message: "feat: initialize Next.js 14 F1 tracker with dark theme and navigation"

4. Create a GitHub repository using `gh repo create` CLI:
   - Name: `f1-2026-tracker` (or similar)
   - Public or private (user's preference -- default to public)
   - Push the main branch

   Command: `gh repo create f1-2026-tracker --public --source=. --remote=origin --push`

   If `gh` CLI is not authenticated, this will fail and the checkpoint below will handle it.

5. Verify the push succeeded: `gh repo view --web` or `git remote -v` shows GitHub URL.
  </action>
  <verify>
    <automated>cd "C:/Users/brand/OneDrive/Desktop/Formula One Race - Brycen" && git remote -v 2>&1 | head -2</automated>
    <manual>Verify GitHub repo exists and contains the project code</manual>
  </verify>
  <done>Git repository exists on GitHub with all project code pushed to main branch.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel with auto-deploy pipeline</name>
  <files></files>
  <action>
1. Install Vercel CLI if not present: `npm install -g vercel`

2. Link and deploy the project:
   - Run `vercel --yes` to deploy (accepts defaults for project setup)
   - This creates the Vercel project and does initial deployment
   - Run `vercel --prod` to promote to production

3. Connect to GitHub for auto-deploy:
   - Run `vercel git connect` to link the Vercel project to the GitHub repo
   - This enables automatic deployments on push to main

4. Capture the production URL from the deploy output.

5. If Vercel CLI requires login, this will trigger the checkpoint below for authentication.

NOTE: If `vercel` CLI approach fails due to auth issues, the alternative is:
- Go to vercel.com/new
- Import the GitHub repo
- Vercel auto-detects Next.js and configures everything
- The checkpoint task below covers this fallback path.
  </action>
  <verify>
    <automated>cd "C:/Users/brand/OneDrive/Desktop/Formula One Race - Brycen" && vercel ls 2>&1 | head -5</automated>
    <manual>Visit the Vercel production URL and confirm the F1 dark theme app loads with navigation</manual>
  </verify>
  <done>Vercel project is deployed and accessible at a live URL. GitHub integration is configured for auto-deploy on push to main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live deployment</name>
  <what-built>Complete F1 tracker skeleton deployed to Vercel with GitHub auto-deploy pipeline. The app should show F1 dark theme with navigation to all sections.</what-built>
  <how-to-verify>
    1. Visit the Vercel production URL (shown in deploy output)
    2. Confirm dark background (#15151E) is visible
    3. Confirm "F1 2026" branding appears in sidebar/header
    4. Click through all 5 nav links (Dashboard, Races, Drivers, Standings, Predictions)
    5. Resize browser to mobile width -- confirm hamburger menu appears and works
    6. On /drivers page, confirm team color swatches are visible
    7. On GitHub, verify the repository exists with committed code
    8. Make a trivial change (e.g., update a placeholder text), push to main, and confirm Vercel auto-deploys within ~60 seconds
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
1. GitHub repository contains all project code on main branch
2. Vercel production deployment is live and accessible
3. Auto-deploy pipeline works (push to main triggers new Vercel build)
4. Live site shows F1 dark theme, navigation, and all placeholder pages
</verification>

<success_criteria>
- GitHub repository is created and contains all project code
- Vercel is deployed with a live production URL
- Push-to-main auto-deploys to Vercel
- User has verified the live site visually and confirmed navigation works
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`
</output>

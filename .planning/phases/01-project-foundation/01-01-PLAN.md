---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/calendar/page.tsx
  - src/app/race/[round]/page.tsx
  - src/app/drivers/page.tsx
  - src/app/standings/page.tsx
  - src/app/predictions/page.tsx
  - src/components/ui/cn.ts
  - src/components/layout/Navigation.tsx
  - src/lib/constants/teams.ts
  - src/lib/constants/theme.ts
  - src/types/index.ts
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - "App runs locally with F1 dark theme (#15151E background, #E10600 red accents, #1F1F2B card surfaces)"
    - "User can navigate between Dashboard, Races, Drivers, Standings, and Predictions via persistent sidebar (desktop) or hamburger menu (mobile)"
    - "All 11 F1 2026 team colors are defined and accessible as constants"
    - "App is responsive -- sidebar on desktop (lg+), collapsible menu on mobile/tablet"
    - "All 6 route pages render without errors"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "F1 theme color tokens (f1-dark, f1-surface, f1-red, f1-border, f1-muted, f1-surface-hover)"
      contains: "f1-dark"
    - path: "src/components/layout/Navigation.tsx"
      provides: "Responsive nav with sidebar and mobile menu"
      min_lines: 50
    - path: "src/lib/constants/teams.ts"
      provides: "All 11 F1 2026 team color constants"
      contains: "cadillac"
    - path: "src/app/layout.tsx"
      provides: "Root layout with dark theme and navigation"
      contains: "bg-f1-dark"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Navigation.tsx"
      via: "import and render in root layout"
      pattern: "import.*Navigation"
    - from: "src/components/layout/Navigation.tsx"
      to: "all page routes"
      via: "Next.js Link components to /, /calendar, /drivers, /standings, /predictions"
      pattern: "href=.*/(calendar|drivers|standings|predictions)"
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "Tailwind theme colors used in CSS and components"
      pattern: "f1-(dark|surface|red)"
---

<objective>
Scaffold the Next.js 14 project with F1 dark theme, team color system, responsive navigation, and all placeholder pages.

Purpose: Establish the complete application skeleton that all future phases build on -- themed, navigable, and responsive.
Output: A locally-runnable Next.js app with F1 branding, 6 route pages, sidebar navigation, and team color constants.
</objective>

<execution_context>
@C:/Users/brand/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 project with F1 theme and team colors</name>
  <files>
    package.json
    tailwind.config.ts
    src/app/globals.css
    src/components/ui/cn.ts
    src/lib/constants/teams.ts
    src/lib/constants/theme.ts
    src/types/index.ts
  </files>
  <action>
1. Run `npx create-next-app@14 . --typescript --tailwind --app --src-dir --eslint` in the project root directory. If prompted, accept defaults. Use `--use-npm` if needed. NOTE: The project root is the current working directory (the "Formula One Race - Brycen" folder). Run the command there so files are created in place, not in a subdirectory.

2. Install Phase 1 supporting deps: `npm install clsx tailwind-merge`

3. Update `tailwind.config.ts` to extend colors with F1 theme tokens:
   - `f1-dark`: '#15151E' (main background)
   - `f1-surface`: '#1F1F2B' (card surfaces)
   - `f1-red`: '#E10600' (F1 red accent)
   - `f1-surface-hover`: '#2A2A3A' (hover state)
   - `f1-border`: '#2E2E3E' (subtle borders)
   - `f1-muted`: '#8B8B9E' (muted text)

4. Replace `src/app/globals.css` with F1 dark theme styles:
   - Tailwind base/components/utilities directives
   - CSS custom properties in `:root` for all F1 colors
   - Body base styles: `@apply bg-f1-dark text-white antialiased`
   - Dark scrollbar styling using webkit and scrollbar-width

5. Create `src/components/ui/cn.ts` with the `cn()` utility combining clsx + tailwind-merge (see research examples).

6. Create `src/lib/constants/teams.ts` with Team interface and TEAMS record for ALL 11 teams (2026 grid):
   - Red Bull (#3671C6), Ferrari (#E80020), Mercedes (#27F4D2), McLaren (#FF8000), Aston Martin (#229971), Alpine (#FF87BC), Williams (#64C4FF), Racing Bulls (#6692FF), Haas (#B6BABD), Audi (#C0C0C0), Cadillac (#FFD700)
   - Include `getTeamStyle()` helper that returns `{ borderColor, '--team-color' }` as CSSProperties
   - Include `getTeamById()` helper for lookups

7. Create `src/lib/constants/theme.ts` exporting F1 theme color hex values as named constants for programmatic use outside Tailwind.

8. Create `src/types/index.ts` with the Team interface (id, name, color, secondaryColor optional).

IMPORTANT: Do NOT use dynamic Tailwind class names for team colors. Team colors MUST use inline `style` attributes. Static F1 theme colors (f1-dark, f1-red, etc.) use Tailwind classes.
  </action>
  <verify>
    <automated>cd "C:/Users/brand/OneDrive/Desktop/Formula One Race - Brycen" && npm run build 2>&1 | tail -5</automated>
    <manual>Check that tailwind.config.ts has f1-dark, f1-surface, f1-red colors defined</manual>
  </verify>
  <done>Next.js 14 project builds successfully. Tailwind config has all F1 theme colors. teams.ts has all 11 teams with correct hex codes. cn() utility exists and exports correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Build root layout, responsive navigation, and all placeholder pages</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/app/calendar/page.tsx
    src/app/race/[round]/page.tsx
    src/app/drivers/page.tsx
    src/app/standings/page.tsx
    src/app/predictions/page.tsx
    src/components/layout/Navigation.tsx
  </files>
  <action>
1. Create `src/components/layout/Navigation.tsx` as a 'use client' component:
   - Define NAV_ITEMS array: Dashboard (/), Races (/calendar), Drivers (/drivers), Standings (/standings), Predictions (/predictions)
   - Desktop (lg+): Fixed left sidebar (w-64) with bg-f1-surface, border-r border-f1-border. Show "F1 2026" branding in f1-red at top. Nav links below with active state (bg-f1-red/10, text-f1-red) based on usePathname(). Inactive links: text-f1-muted, hover:text-white, hover:bg-f1-surface-hover
   - Mobile (below lg): Fixed top bar with "F1 2026" branding and hamburger button. Clicking hamburger opens a slide-out overlay nav with same items. Close on nav item click or overlay backdrop click. Use useState for open/close toggle.
   - Use cn() utility for conditional classes
   - Use Next.js Link components (NOT <a> tags)
   - Include simple SVG icons or emoji icons for each nav item to improve visual clarity

2. Update `src/app/layout.tsx`:
   - Import Inter from next/font/google
   - Set metadata: title "Brycen's F1 2026 Tracker", description "Track the 2026 Formula One season"
   - Render html with lang="en" and className="dark"
   - Render body with Inter font class + `bg-f1-dark text-white min-h-screen`
   - Flex container: Navigation component + main content area with `flex-1 p-4 md:p-6 lg:ml-64`
   - Add top padding on mobile to account for fixed mobile header (pt-16 lg:pt-0)

3. Create placeholder pages -- each should have:
   - A heading (text-2xl md:text-3xl font-bold)
   - A muted description (text-f1-muted) saying "Coming in Phase N"
   - A simple visual element using F1 theme colors to verify theme is working (e.g., a card with bg-f1-surface and border-f1-border)

   Pages to create:
   - `src/app/page.tsx` -- "Dashboard" (Coming in Phase 9) with a sample card showing f1-surface background
   - `src/app/calendar/page.tsx` -- "Race Calendar" (Coming in Phase 3)
   - `src/app/race/[round]/page.tsx` -- "Race Detail" (Coming in Phase 4), receives params.round
   - `src/app/drivers/page.tsx` -- "Drivers" (Coming in Phase 5), include a small team color demo showing 3-4 team color swatches using inline styles from teams.ts to verify team colors work
   - `src/app/standings/page.tsx` -- "Standings" (Coming in Phase 6)
   - `src/app/predictions/page.tsx` -- "Predictions" (Coming in Phase 7)

IMPORTANT: The Navigation component MUST use 'use client' directive (it uses useState and usePathname hooks). All placeholder pages can be Server Components (no directive needed).
  </action>
  <verify>
    <automated>cd "C:/Users/brand/OneDrive/Desktop/Formula One Race - Brycen" && npm run build 2>&1 | tail -10</automated>
    <manual>Run `npm run dev` and verify: (1) dark background visible, (2) sidebar shows on wide viewport, (3) clicking each nav item loads correct page, (4) mobile hamburger works on narrow viewport</manual>
  </verify>
  <done>App builds and runs. Root layout applies F1 dark theme globally. Navigation sidebar shows 5 links on desktop, hamburger menu on mobile. All 6 pages render with correct headings. Active nav link is highlighted in F1 red. Drivers page shows team color swatches proving team constants work.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. `npm run dev` serves the app at localhost:3000
3. All 6 routes are accessible: /, /calendar, /drivers, /standings, /predictions, /race/1
4. Dark theme (#15151E background) is visible on all pages
5. Navigation highlights the active page
6. Sidebar collapses to hamburger on mobile viewport
7. Team color swatches on /drivers page render with correct colors
</verification>

<success_criteria>
- Next.js 14 project with TypeScript and Tailwind CSS runs locally
- F1 dark theme colors applied globally (#15151E bg, #E10600 red, #1F1F2B surfaces)
- All 11 team colors defined in constants file with correct hex values
- Responsive navigation: sidebar on desktop (lg+), hamburger on mobile
- All 5 navigation links work and route to correct pages
- 6 route pages render without errors
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
